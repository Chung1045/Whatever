<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Whatever" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:fontFamily">"@font/inter_variable"</item>
    </style>

    <style name="Theme.Whatever" parent="Base.Theme.Whatever" />

    <style name="Theme.Whatever.Button" parent="ThemeOverlay.Material3.Button">
        <item name="android:fontFamily">@font/inter_variable</item>
        <item name="android:backgroundTint">@color/buttonBackground</item>
    </style>

    <style name="Theme.Whatever.ResultButton" parent="ThemeOverlay.Material3.Button">
        <item name="android:backgroundTint">@color/resultScreenButtonBackground</item>
        <item name="android:drawableTint">@color/resultScreenButtonForeground</item>
    </style>


</resources>